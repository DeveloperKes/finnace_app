/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LangPipe } from './lang.pipe';

describe('Pipe: Lange', () => {
  it('create an instance', () => {
    let pipe = new LangPipe();
    expect(pipe).toBeTruthy();
  });
});
